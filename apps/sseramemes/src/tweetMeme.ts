import { MessageReaction, PartialMessageReaction } from 'discord.js';

export const tweetMeme = async (
  message: MessageReaction | PartialMessageReaction,
) => {
  return { tweetUrl: 'tweetUrl' };
};
